#!/usr/bin/perl

# shows a specific folder - specially the sent mails folder
# this method runs when indes.pl is loaded 
# first ist displays the undread mails and the then the last 100 read mails
exit 0