#!/usr/bin/perl

# moves a mail to a specific folder

# a folder has an ID. the mail has an ID as well. 
# if the folder is the same as the one that is clickt, then nothing happens. otherwise
# the mail is removed on the browser window


exit 0